# QUANT_GUARDRAILS — 퀀트 트레이딩 함정 회피 가드레일

> 🚨 **모든 전략·백테스트·데이터 수집 코드 작성 전에 이 문서의 해당 섹션을 점검한다.**
> 코딩 능력보다 **함정을 피하는 것**이 1순위. 백테스트 결과가 좋아 보일수록 이 문서의 체크리스트를 더 엄격히 적용해야 한다.
> 요약은 `CLAUDE.md` "퀀트 가드레일" 섹션에 있고, 본 문서는 상세 체크리스트와 코드 예시를 담는다.

---

## 1. 과최적화 (Overfitting)

> "백테스트는 과거에 대한 완벽한 설명, 미래에 대한 완벽한 무지를 만들 수 있다."

### ❌ 함정
- 파라미터 100개 그리드 서치로 가장 좋은 조합 채택
- 한 자산·한 기간에서만 잘 작동하는 전략
- Sharpe 3+, MDD <5%, 승률 70%+ — 이 정도면 거의 100% 과적합
- "이 시점에 진입했으면 +400%" 류의 사후 해설

### ✅ 가드레일
- **인-샘플(IS) / 아웃-오브-샘플(OOS) 분리 필수**: 보통 70/30 또는 시간 기반 split
- **Walk-forward validation**: rolling window로 학습→검증 반복
- **파라미터 수 최소화**: lookback 1개, threshold 1개 정도. 3개 넘어가면 의심
- **다중 자산·다중 기간 검증**: KOSPI 200, KOSDAQ 150, 미국 S&P 500에서도 비슷한 결과 나오는지
- **랜덤 베이스라인 비교**: 동일 자산을 매주 무작위 N개 매수 vs 전략. 통계적으로 우월해야 함
- **단순한 게 강하다**: 복잡한 전략이 단순한 전략(buy & hold, 60/40)을 못 이기면 폐기
- **거래 횟수 ≥ 30**: 그 미만은 통계적 의미 없음 (대수의 법칙 미발동)

### 🔴 즉시 폐기 신호
- IS Sharpe 2.0 → OOS Sharpe 0.3 이하: 과적합 확정
- 파라미터 1개만 바꿔도 성과가 완전히 무너짐: 과적합
- 특정 1~2개 종목에서만 수익이 발생함: 과적합

---

## 2. 생존 편향 (Survivorship Bias)

> "현재 살아있는 종목만 보면, 죽은 종목들의 무덤은 보이지 않는다."

### ❌ 함정
- 오늘의 KOSPI 200 구성종목으로 5년 전 백테스트 → 5년 전 시점에는 다른 종목 구성이었음
- 상장폐지 종목(테마주, 부실기업)이 데이터에서 빠짐 → 실제 손실 안 잡힘
- "10년 전부터 보유했으면 +1000%"의 환상

### ✅ 가드레일
- **시점별(point-in-time) 종목 마스터 사용**: `pykrx.get_index_portfolio_deposit_file(date)`로 매 리밸런싱 시점의 KOSPI 200 구성을 새로 조회
- **상장폐지 종목 데이터도 가능하면 포함**: pykrx는 상장폐지 종목 일봉도 일부 제공 (`get_market_ohlcv_by_date`로 직접 조회 가능)
- **종목 변경 이력 추적**: 인덱스 구성 변경일을 기록하고, 백테스트는 변경일 이후 새 구성 사용
- **편입/제외 시 가격 처리**: 편입은 직전일 종가에 매수, 제외는 제외 당일 종가에 매도

### 코드 패턴
```python
# ❌ 잘못된 방식
tickers_today = fetch_kospi200_tickers(date.today())
for backtest_date in date_range:
    # 5년 전 데이터에 오늘의 종목 사용 — 생존 편향!
    prices = load_prices(tickers_today, backtest_date)

# ✅ 올바른 방식
for rebalance_date in monthly_rebalance_dates:
    tickers_at_t = fetch_kospi200_tickers(rebalance_date)
    prices = load_prices(tickers_at_t, rebalance_date)
```

---

## 3. 미래 참조 편향 (Look-ahead Bias)

> "내일의 종가로 오늘 진입했다면, 시장은 너에게 무한한 알파를 줄 것이다."

### ❌ 함정
- 시그널 계산에 t 시점 종가 사용 → 그 종가로 t에 진입 (불가능)
- 실적 발표일(공시일)이 아닌 "발표 분기 종료일"에 펀더멘털 데이터 적용
- pandas `.rolling()` 후 `.shift()` 빠뜨림
- 미래 데이터로 학습한 ML 모델로 과거 시점 예측

### ✅ 가드레일
- **시그널 = t-1까지 정보, 진입 = t 시가 (또는 t 종가)**
- **모든 시계열 계산 후 `.shift(1)` 누락 점검**: rolling, ewm, 모멘텀 모두 적용
- **재무 데이터는 발표일(공시일) 기준**: 분기 종료일이 아니라 공시일 + 보수적으로 1~3일 지연
- **벡터화 코드 리뷰**: `vectorbt`/`pandas` 벡터 연산 시 무심코 미래 정보 섞이기 쉬움
- **자가 점검 질문**: "이 라인이 실행될 때, 이 정보를 정말 알 수 있었는가?"

### 코드 패턴
```python
# ❌ 미래 참조
returns = price.pct_change()
signal = returns > 0          # 오늘 수익률로 오늘 진입 판단 — 불가능
position = signal.astype(int)
strategy_return = returns * position

# ✅ 지연 적용
returns = price.pct_change()
signal = (returns.shift(1) > 0)  # 어제 수익률 보고 오늘 진입
position = signal.astype(int)
strategy_return = returns * position
```

---

## 4. 거래 비용 (Transaction Costs)

> "백테스트에서는 무료로 거래하지만, 실거래에서는 매번 돈을 낸다."

### ❌ 함정
- 수수료/거래세를 0으로 설정한 백테스트
- 슬리피지 무시 (특히 소형주)
- 일중 변동성을 사용한 전략이 비용 차감 후 손실
- 호가 단위(tick size) 무시한 진입가 계산

### ✅ 가드레일

**한국 주식 비용 구조 (2025 기준)**

| 항목 | 매수 | 매도 |
|---|---|---|
| 거래세 (KOSPI/KOSDAQ) | 0% | 0.18% |
| KIS 비대면 매매수수료 | 0.0036~0.015% | 0.0036~0.015% |
| 슬리피지 (우량주) | 0.05% | 0.05% |
| 슬리피지 (중소형주) | 0.1~0.5% | 0.1~0.5% |
| **왕복 비용 (보수적)** | — | **0.25~0.5%** |

- **백테스트 비용은 항상 보수적으로**: 우량주 0.25%, 중소형 0.5% 왕복부터 시작
- **회전율 × 비용 = 연간 비용 부담**: 월 회전율 100%면 연 12회 → 연 3~6% 비용
- **비용 차감 후에도 알파가 남는지** 확인. 차감 전만 보면 무용
- **최소 진입 단위(1주) 고려**: 시드 100만 원으로 70만 원짜리 종목 분산 불가
- **호가 단위(tick size)** 반영: 1,000원 미만 1원, 5,000원 미만 5원, 등...

### 코드 패턴
```python
# vectorbt 비용 설정 예
import vectorbt as vbt

portfolio = vbt.Portfolio.from_signals(
    close=prices,
    entries=entries,
    exits=exits,
    fees=0.00015,        # KIS 매매수수료 한쪽
    slippage=0.001,      # 슬리피지 한쪽 0.1%
    # 거래세는 매도 시만 → 별도 후처리 권장
    init_cash=10_000_000,
    size=vbt.Size.Percent,
    size_type=vbt.SizeType.Percent100,
)
# 매도 시 거래세 0.18% 추가 차감 후 평가
```

---

## 5. Crowding (트레이더 효과 / 알파 소멸)

> "많은 사람이 사용하는 전략은 더 이상 알파가 아니다."

### ❌ 함정
- 책/논문에 자주 나오는 전략에 큰 자본 베팅
- 한국 시장에서 외국인/기관이 사용 가능한 전략 (대형주 모멘텀, PER/PBR 가치)
- "다들 쓰는 RSI/MACD" 기반 단순 전략

### ✅ 가드레일
- **알파의 출처 자문**: "왜 이 알파가 아직 존재하는가? 누가 손해를 보고 있는가?"
- **유명한 전략은 베이스라인으로만 사용**: 개선·결합·필터링 후 차별화
- **소형주·비유동성 종목**: 기관이 들어오기 어려운 영역 (단, 슬리피지·유동성 함정 주의)
- **시간 비대칭 신호**: 분기 실적 공시 직후 24시간 같은 단기 윈도우
- **백테스트 기간 중 전략 수익률 추세**: 최근으로 올수록 성과 약해지면 알파 소멸 진행 중

### 한국 시장 특수성
- 외국인/기관 비중 높은 대형주 → Crowding 강함
- 개인투자자 비중 높은 코스닥 소형주 → 비효율 존재 가능 (다만 유동성/조작 위험)
- 테마주·작전주 → 백테스트 통계 신뢰 불가, 전략에서 제외 권장

---

## 6. 데이터 품질

> "Garbage in, garbage out — 무료 데이터는 무료인 이유가 있다."

### ❌ 함정
- 결측치(NaN)를 0이나 직전 값으로 무지성 채움
- 액면분할/배당 미조정 가격으로 수익률 계산
- 거래정지일이 0거래량으로 잡혀서 수익률 0% 처리
- 단일 데이터 소스 의존 (오류·누락 검증 불가)

### ✅ 가드레일
- **수정주가(adj_close) 사용**: pykrx는 액면분할/배당 조정된 수정주가 제공. 비조정 가격으로 수익률 계산 금지
- **결측치 처리 정책 명시**: forward-fill 금지(미래 참조 위험), 결측 = 거래정지/상폐로 처리
- **이상치 탐지**: 일일 수익률 ±30% 이상은 검토 필요 (액면분할 미반영, 데이터 오류 가능성)
- **거래정지 종목**: `get_market_ohlcv_by_date` 결과의 거래량 0인 날 식별, 시그널/진입에서 제외
- **교차 검증**: pykrx + FinanceDataReader 결과 비교, 불일치 시 KRX 공식 데이터 우선
- **데이터 freshness 체크**: 최신 일자가 며칠 전이면 경고 (휴장 vs 데이터 누락 구분)

### 코드 패턴
```python
# ❌ 위험
df["close"] = df["close"].fillna(method="ffill")  # 미래 참조 위험

# ✅ 안전
df["is_traded"] = df["volume"] > 0
df.loc[~df["is_traded"], ["open", "high", "low", "close"]] = np.nan
# 시그널 계산 시 NaN인 종목은 자동 제외됨
```

---

## 7. 통계적 유의성

> "거래 횟수가 적으면 행운과 실력을 구분할 수 없다."

### ❌ 함정
- 5년 백테스트에서 거래 12번 → "Sharpe 2.5!" → 통계적 의미 없음
- 한 번의 큰 거래가 전체 수익률을 좌우
- p-value, 신뢰구간 미고려

### ✅ 가드레일
- **최소 거래 횟수 30 이상**: 그 미만은 통계 무효
- **수익률 분포 확인**: 단일 거래 기여도가 전체의 30% 넘으면 위험
- **부트스트랩(bootstrap)**: 거래 결과를 재샘플링해서 Sharpe 신뢰구간 계산
- **Deflated Sharpe Ratio (DSR)**: Lopez de Prado 제안. 다중 비교 보정
- **Monte Carlo 시뮬레이션**: 거래 순서를 무작위로 섞어 MDD 분포 확인

---

## 8. 한국 시장 특화 함정

### 단주 체결 / 호가 단위
- 매매 단위는 1주이지만 호가 단위(tick)는 가격대별 다름
- 0~2,000원: 1원 / 2,000~5,000원: 5원 / 5,000~20,000원: 10원 / ...
- 시그널 가격을 호가 단위로 반올림하지 않으면 백테스트가 비현실적

### 유동성 필터 (필수)
- 일평균 거래대금 < 10억 원 종목은 슬리피지 폭발 → 제외 권장
- 일평균 거래량 < 10만 주 종목도 제외
- ETF/우선주는 별도 필터링 (추적오차, 유동성 격차)

### 거래정지 / 단기과열 / 투자주의
- 거래정지일 진입/청산 불가 → 시그널 발생해도 다음 영업일까지 대기
- 단기과열 종목(3일 단일가) → 슬리피지 극심
- 관리종목/투자주의 환기 → 보통 백테스트에서 제외

### 사이드카 / 서킷브레이커
- KOSPI 200 선물 5%/10% 변동 시 사이드카/서킷브레이커
- 발동 시 5분 매매정지 → 자동매매 시 주문 큐에 쌓이지 않게 처리

### 시장 구조
- 정규장: 09:00~15:30 (점심시간 없음, 동시호가 08:30~09:00, 15:20~15:30)
- 시간외 단일가: 16:00~18:00 (10분 단위)
- VI(변동성 완화장치): 종목별 ±10% 발동 시 2분 단일가
- 자동매매에서 동시호가/VI 시점 주문은 별도 로직 필요

### 외국인·기관 한도
- 일부 종목 외국인 보유 한도 존재 (KT, 한전 등)
- 자동매매에서 한도 초과 주문은 거부됨

---

## 9. 리스크 관리 (Risk First)

> "수익은 옵션이지만, 손실 제한은 의무다."

### ✅ 필수 항목
- **단일 종목 노출 한도**: 보통 10~20% (시드 작으면 30%까지 허용)
- **포트폴리오 MDD 한도**: 백테스트 MDD가 시드 대비 30% 넘으면 라이브 금지
- **일일 손실 한도(Kill switch)**: 일중 -3% 도달 시 당일 추가 진입 중단
- **레버리지 사용 금지**: 시드 단계에선 신용/대주 절대 사용 금지
- **포지션 사이징**: 켈리 공식 사용 시 1/4 켈리 (Full Kelly는 변동성 너무 큼)
- **상관관계**: 종목 간 상관관계 0.8 이상이면 분산 효과 없음 → 하나만 선택

---

## 10. 라이브 진입 전 체크리스트

전략을 실거래로 옮기기 전 모두 통과해야 한다.

- [ ] IS/OOS 분리 검증 통과 (OOS Sharpe > 0.5)
- [ ] Walk-forward 검증 통과 (구간별 일관된 수익)
- [ ] 거래 횟수 ≥ 30
- [ ] 거래세·수수료·슬리피지 차감 후 양의 수익
- [ ] 다중 자산/다중 기간 검증
- [ ] 단순 베이스라인(buy & hold) 대비 우월
- [ ] MDD가 시드 대비 감내 가능 (< 30%, 또는 시드 분할로 가중 MDD < 30%)
- [ ] 단일 종목 기여도 < 30%
- [ ] Kill switch / 일일 손실 한도 코드 동작 확인
- [ ] 모든 의존 외부 API의 장애·rate limit 처리 코드 존재
- [ ] 알람·모니터링 (텔레그램/이메일) 동작 확인

**하나라도 미통과면 라이브 진입 보류.**

---

## 11. 코드 작성 시 자기 점검 질문

전략·백테스트 코드 한 줄 한 줄 작성할 때 다음을 자문한다:

1. **이 시점에 이 정보를 정말로 알 수 있었는가?** (미래 참조)
2. **이 데이터셋은 그 시점에 존재하던 종목만 포함하는가?** (생존 편향)
3. **이 비용 모델은 실거래보다 보수적인가?** (낙관 편향)
4. **이 결과가 너무 좋다면, 무엇이 잘못되었는가?** (과적합)
5. **이 수익은 누구의 손실인가? 왜 그 사람은 계속 손실을 보는가?** (Crowding)
6. **거래 횟수가 충분한가?** (통계적 유의성)
7. **최악의 경우 얼마나 잃을 수 있는가?** (리스크 관리)

---

## 참고
- López de Prado, *Advances in Financial Machine Learning* (2018) — 백테스트 함정의 정수
- Bailey & López de Prado, *The Deflated Sharpe Ratio* (2014)
- Harvey, Liu, Zhu, *... and the Cross-Section of Expected Returns* (2016) — 다중 비교 함정
